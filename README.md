# Todo List App - React + TypeScript + Material UI

This is a **Todo List** project built with **React** and **TypeScript**, using **Material UI** for the interface. It allows you to add, complete, and delete tasks, with persistence in **localStorage** and support for **light/dark mode**.

---

## 🔹 Features

- Add new tasks
- Mark tasks as completed
- Delete tasks
- Data persistence using `localStorage`
- Modern interface with **Material UI**
- Light/Dark mode toggle
- Responsive and clean design

---

## 🔹 Technologies

- **React**  
- **TypeScript**  
- **Material UI (MUI)**  
- **CSS** for layout and basic animations  
- **LocalStorage** for task persistence

---

## 🔹 Installation

1. Clone the repository:

```bash
git clone https://github.com/dalbacarrasco/to-do-list-react.git
cd todo-list-react

2. Install dependencies:

```bash
npm install

3. Run the app in development mode:

```bash
npm npm start

4. Open your browser at the URL shown in the terminal (usually http://localhost:5173/).


